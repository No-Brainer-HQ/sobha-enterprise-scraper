{
    "title": "Enterprise Sobha Portal Scraper",
    "description": "Bulk scraping of all Sobha properties with minimal configuration required.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "email": {
            "title": "Sobha Portal Email",
            "type": "string",
            "description": "Your Sobha Partner Portal email address.",
            "editor": "textfield"
        },
        "password": {
            "title": "Sobha Portal Password",
            "type": "string",
            "description": "Your Sobha Partner Portal password.",
            "editor": "textfield",
            "isSecret": true
        },
        "maxResults": {
            "title": "Maximum Properties to Scrape",
            "type": "integer",
            "description": "Leave as 5000 to get all available properties.",
            "default": 5000,
            "minimum": 100,
            "maximum": 10000
        },
        "enableStealth": {
            "title": "Enable Anti-Detection",
            "type": "boolean",
            "description": "Keep enabled to avoid being blocked.",
            "default": true
        }
    },
    "required": ["email", "password"]
}
